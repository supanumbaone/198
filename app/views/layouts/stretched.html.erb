<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "CSE 110 Grouper" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "jquery", "rails", "https://ajax.googleapis.com/ajax/libs/jqueryui/1.8.11/jquery-ui.min.js", "application" %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body id="stretched_body">
    <div id="stretched_container">
	  <%= render 'layouts/header' %>
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= content_tag :h1, yield(:title) if show_title? %>
      <%= yield %>
    </div>
  </body>
</html>
