<%= form_for [@group, @comment] do |f| %>
  <%= f.error_messages %>
  <%= hidden_field_tag :parent_id, params[:parent_id] %>
  <p>
     <%= f.label :body %><br />
     <%= f.text_area :body %>
  </p>
  <p><%= f.submit %></p>
<% end %>
